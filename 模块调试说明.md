# 编译宏与模块调试

宏定义位于 `src/module_flags.h`，取值只能为 0 或 1。默认全部为 1，保持原有行为。
可以直接修改头文件，也可以通过下面的 make 参数覆盖；修改后需要重新编译并重启应用。

| make 参数 | 对应宏 | 控制功能 |
| --- | --- | --- |
| ENABLE_INFER | CA_ENABLE_INFER | BSC/VIN 取原始帧、mscale、HIKFlow 模型检测 |
| ENABLE_RING | CA_ENABLE_RING | RTSP 拉取编码流以及内存环形缓存 |
| ENABLE_CLIP | CA_ENABLE_CLIP | 异常事件队列、前后时间窗、本地视频切片 |
| ENABLE_UPLOAD | CA_ENABLE_UPLOAD | HTTP 上传线程、重试及返回确认 |
| ENABLE_LEGACY_ALARM | CA_ENABLE_LEGACY_ALARM | 原 demo 报警线程、音频初始化、报警 JPEG 与 HEOP 事件联动 |

基础检测使用 VIN 原始帧，不依赖 RTSP。环形缓存保存的是 RTSP 编码数据，和模型输入的原始帧是两路数据。
切片必须开启缓存，上传必须开启切片；不合法的组合会编译报错。
关闭旧报警联动仍保留模型判断、本项目事件桥接、POS 目标框和标签提交，以及必要的平台初始化。
因此关闭旧报警联动并不保证消除所有平台鉴权或通道相关日志。

## 常用组合

在原 HEOP 编译容器内、项目根目录执行。

只调试基本检测：

```sh
make CC=aarch64-mix210-linux-gcc ENABLE_INFER=1 ENABLE_RING=0 ENABLE_CLIP=0 ENABLE_UPLOAD=0 ENABLE_LEGACY_ALARM=0
```

只调试 RTSP 和环形缓存，不初始化模型：

```sh
make CC=aarch64-mix210-linux-gcc ENABLE_INFER=0 ENABLE_RING=1 ENABLE_CLIP=0 ENABLE_UPLOAD=0 ENABLE_LEGACY_ALARM=0
```

不运行模型，使用测试事件调试本地切片：

```sh
make CC=aarch64-mix210-linux-gcc ENABLE_INFER=0 ENABLE_RING=1 ENABLE_CLIP=1 ENABLE_UPLOAD=0 ENABLE_LEGACY_ALARM=0
```

此时在 app.conf 中设置：

```ini
test_event_interval_seconds=30
debug_level=1
```

模型关闭后，infer_thread 仅承担测试事件的定时产生，不会启动 HIKFlow。
第一条测试事件在启动后立即产生，可能缺少前置视频；检查后续事件的完整时间窗。
切片和 JSON 保留在 `<work_dir>/events`，不创建上传队列，不等待服务器。
本地切片没有自动清理策略，长时间测试要关注存储空间。

将上述命令中的 ENABLE_UPLOAD 改为 1，并配置 upload_url，即可在没有模型的情况下调试视频上传。
当前上传测试仍以生成的切片为输入，没有增加“只上传一个已有文件”的独立入口。

开启完整检测、缓存、切片、上传，关闭旧 demo 联动：

```sh
make CC=aarch64-mix210-linux-gcc ENABLE_INFER=1 ENABLE_RING=1 ENABLE_CLIP=1 ENABLE_UPLOAD=1 ENABLE_LEGACY_ALARM=0
```

真实检测时设置 `test_event_interval_seconds=0`，模型路径、异常类别和检测间隔继续由 app.conf 控制。

## 编译、部署与检查

执行 make install 时必须带上与编译时相同的 ENABLE_* 参数，否则会恢复头文件默认值并重新构建。
Makefile 会记录宏参数变化，重新编译所有对象，包括 vendor 中受宏影响的代码。修改头文件也会重新编译。

启动日志会输出：

```text
modules: infer=1 ring_rtsp=0 clip=0 upload=0 legacy_alarm=0
```

主工程日志输出到 stderr，由现有启动环境收集；HIKFlow 日志继续使用原 DSP 日志通道。
关闭切片时不会创建事件队列，模型报警只记录日志，不会向无人消费的队列发送事件。
关闭上传时不会创建上传线程或队列，本地切片仍正常生成。

这些宏控制模块执行和资源分配，不负责裁剪 SDK 链接依赖。即使只调试 RTSP，当前 Makefile 仍需要原有 HEOP 工具链和库。
切换组合后要重启进程；不支持在同一进程内热启停 vendor 的内部线程。

本次未在球机上实测；当前 Windows 环境没有可用的交叉编译器，Docker 接口访问被拒绝，尚未完成容器编译验证。

```bash
sh makeapp.sh CC=aarch64-mix210-linux-gcc \
  ENABLE_INFER=1 ENABLE_RING=0 ENABLE_CLIP=0 \
  ENABLE_UPLOAD=0 ENABLE_LEGACY_ALARM=0
```

