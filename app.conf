camera_id=ptz_001
# 127.0.0.1 is resolved to the HEOP-provided BR0_IP at runtime.
rtsp_url=rtsp://127.0.0.1:554/ISAPI/Streaming/channels/101
upload_url=http://192.168.1.2:8080/api/upload
work_dir=/heop/package/cameraAbnormal/user_data/camera_abnormal
# Empty hikflow_model_path keeps the model_path from hikflow_config.json.
hikflow_model_path=
# Empty abnormal_classes keeps original sel_class behavior. Example: 1,2,3 means class 0 is normal.
abnormal_classes=
human_alarm_ip=10.184.142.15
human_alarm_port=7200
listen_port=0
app_chan=-1
pre_seconds=5
post_seconds=10
ring_seconds=30
max_events=4
upload_retry=3
upload_retry_interval_ms=3000
cooldown_seconds=20
infer_interval_seconds=5
codec=auto
fps=25
# 0=debug off, 1=key debug logs, 2=verbose packet/queue stats.
debug_level=0
# For integration testing only. Set 0 in production.
test_event_interval_seconds=0
